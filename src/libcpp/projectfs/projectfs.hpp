#ifndef NABLA_PROJECT_FS_HPP
#define NABLA_PROJECT_FS_HPP

namespace NABLA
{
    class ProjectFS
    {
        public:

            enum class ProjectType
            {
                UNKNOWN,
                LIBRARY,
                PROJECT
            };

            enum class LoadReturn
            {
                
            };

            ProjectFS();
            ~ProjectFS();



        private:
    };
}

#endif