language: cpp
dist: bionic

# More builds expected in the future
matrix:
  include:
    - os: linux
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-8
      env:
        - MATRIX_EVAL="CXX_COMPILER=g++-8"
    
before_install:
  - eval "${MATRIX_EVAL}"

  - PARENTDIR=$(pwd)
  
  - mkdir $PARENTDIR/build
  - mkdir $PARENTDIR/build-release

  - bash install_cpputest.sh

install:

script:

  - cd $PARENTDIR/build
  - cmake $PARENTDIR/src -DCMAKE_BUILD_TYPE=Debug
  - make -j9

  - cd $PARENTDIR/build-release
  - cmake $PARENTDIR/src -DCMAKE_BUILD_TYPE=Release
  - make -j9
